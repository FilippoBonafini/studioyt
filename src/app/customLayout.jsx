const CustomLayout = ({ children }) => (
    <div className="custom-layout">
        {/* Altri elementi del layout, come l'intestazione, la barra laterale, ecc. */}
        {children}
        {/* Altri elementi del layout, come il piè di pagina, ecc. */}
    </div>
);

export default CustomLayout;